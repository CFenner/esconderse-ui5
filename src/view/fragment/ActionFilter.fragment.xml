<core:FragmentDefinition
  xmlns="sap.m"
  xmlns:core="sap.ui.core">
  	<ActionSheet
		title="{i18n>pageMaster.footer.filter.title}"
		placement="Top">
		<buttons>
	  		<Button 
	  			icon="sap-icon://undo" 
	  			text="{i18n>pageMaster.footer.filter.none.title}" 
	  			tooltip="{i18n>pageMaster.footer.filter.none.tooltip}" 
	  			press="onFilterReset"/>	
	  		<Button 
	  			icon="sap-icon://connected" 
	  			text="{i18n>pageMaster.footer.filter.active.title}" 
	  			tooltip="{i18n>pageMaster.footer.filter.active.tooltip}" 
	  			press="onFilterActive"/>
	  		<Button 
	  			icon="sap-icon://disconnected" 
	  			text="{i18n>pageMaster.footer.filter.inactive.title}" 
	  			tooltip="{i18n>pageMaster.footer.filter.inactive.tooltip}" 
	  			press="onFilterInactive">
	  			<customData>
	  				<core:CustomData key="status" value="inactive"/>
	  			</customData>
	  		</Button>
    	</buttons>
	</ActionSheet>
</core:FragmentDefinition>
