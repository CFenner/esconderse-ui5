<mvc:View
	xmlns="sap.m" 
	xmlns:core="sap.ui.core"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:html="http://www.w3.org/1999/xhtml">
  <Page
    showHeader="false"
    class="sapUiFioriObjectPage" >    
    <content>
      <ObjectHeader
        title="{account>/email}">
        <attributes>
          <ObjectAttribute title="Angelegt" text="{account>/created}" />
        </attributes>
      </ObjectHeader>
      <IconTabBar
        expanded="{device>/isNoPhone}">
        <items>
          <IconTabFilter
            text="Profil"
            icon="sap-icon://customer"
            tooltip="detailProfileTabPersonalTooltip">
            
            <Table
            	items="{account>/permission/grants}">
    <columns>
      <Column>
        <Text text="{i18n>welcomeColumnHeaderReason}" />
      </Column>
      <Column>
        <Text text="{i18n>welcomeColumnHeaderCount}" />
      </Column>
      <Column>
        <Text text="{i18n>welcomeColumnHeaderDate}" />
      </Column>
    </columns>
   				 <items>
      				<ColumnListItem>
				        <cells>
				          <ObjectIdentifier
				            title="{account>description}"/>
				          <Text
				            text="{account>count}" />
				          <Text
				            text="{account>prettyDate}" 
				            tooltip="{account>createDate}"/>
    			   		</cells>
			      </ColumnListItem>
			    </items>
            </Table>
            
          </IconTabFilter>
          <IconTabFilter
            text="Weiterleitungen"
            icon="sap-icon://email"
            tooltip="detailProfileTabEmailTooltip">
		<ProgressIndicator
        width="100%"
        percentValue="{account>/permission/max}"
        displayValue="{account>/permission/max}"
        showValue="true"
        state="Success" />
          </IconTabFilter>
        </items>
      </IconTabBar>
    </content>
  </Page>
</mvc:View>